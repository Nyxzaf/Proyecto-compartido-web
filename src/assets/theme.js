import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#CB4335",
      dark: "#922B21",
    },
    secondary: {
      main: "#CB4335",
    },
  },
});

export default theme;
