import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#CB4335",
      dark: "#922B21",
      card: "#EBEBEB",
    },
    secondary: {
      main: "#CB4335",
      pink:"#e3bebf"
    },
  },
});

export default theme;
